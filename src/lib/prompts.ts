import { ContentType } from '../types';

export const buildManualPrompt = (niche: string, count: number) => `You are an expert SEO strategist and topical authority architect.\n\nFor the niche: ${niche}, generate exactly ${count} pillar page topics that will form the foundation of a topical authority website. Each pillar should represent a major subtopic within this niche.\n\nFor each pillar, provide:\n- title: A compelling pillar page title that includes the main keyword\n- description: A one-sentence description of the page's purpose\n- urlSlug: SEO-friendly URL slug (lowercase, hyphens only, no special characters)\n- focusKeyword: The primary keyword this pillar targets\n- estimatedSearchVolume: An estimated monthly search volume range as a string (e.g. '1,000-5,000')\n\nReturn ONLY a valid JSON array. No markdown, no explanation, no code blocks. Just the raw JSON array.`;

export const buildBriefPrompt = (params: { type: ContentType; title: string; keyword: string; niche: string; pillar?: string; }) =>
`You are a senior content strategist who creates detailed SEO-optimized article briefs.\n\nCreate a detailed article brief for the following ${params.type} article:\n\nTitle: ${params.title}\nFocus Keyword: ${params.keyword}\nNiche: ${params.niche}\nArticle Type: ${params.type}\n${params.type === 'cluster' ? `Parent Pillar: ${params.pillar}` : ''}\n\nProvide the following as a single JSON object:\n- meta_description: A 150-155 character meta description that includes the focus keyword naturally\n- secondary_keywords: An array of 5-8 LSI and semantically related keywords to naturally include in the article\n- word_count_target: Recommended target word count as an integer (1500-3000 for clusters, 3000-5000 for pillars)\n- outline: A detailed array of section objects, each with: level ("h2" or "h3"), heading (the heading text), notes (what to cover in this section, 1-2 sentences)\n\nReturn ONLY valid raw JSON, no markdown, no explanation.`;
